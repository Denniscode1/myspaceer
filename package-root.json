{
  "name": "myspaceer-fullstack",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && cd server && npm install",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}